# Pip-installable dependency
matplotlib==3.10.8

# Required runtime module
# tkinter
# Note: tkinter is part of the Python standard library / Python installer,
# so it is required by this project but is not installed via pip.
